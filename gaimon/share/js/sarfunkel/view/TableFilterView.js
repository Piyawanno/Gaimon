class TableFilterView{
	constructor(table){
		this.table = table;
		this.page = table.page;
		this.meta = table.page.meta;

		this.filter = null;
	}

	async render(){
		if(this.filter == null){
		}
	}
}